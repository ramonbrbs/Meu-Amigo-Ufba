<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xForms="clr-namespace:Syncfusion.ListView.XForms;assembly=Syncfusion.SfListView.XForms"
             x:Class="MeuAmigo.Views.Inicial">
    <ContentPage.Content>
        <StackLayout Spacing="0">


            <ListView x:Name="LstContatosPendentes" RowHeight="40" VerticalOptions="Start" SeparatorVisibility="None">
                <ListView.ItemTemplate >

                    <DataTemplate>
                        <ViewCell>
                            <Label Text="{Binding Nome}" FontSize="Medium" TextColor="Black"/>
                        </ViewCell>


                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            
            

            <Label HorizontalOptions="CenterAndExpand" Text="You have no contacts yet." IsVisible="False" x:Name="LblSemItens"></Label>
            <xForms:SfListView x:Name="LstContatos"></xForms:SfListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>